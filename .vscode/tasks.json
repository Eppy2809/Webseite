{
  "version": "2.0.0",
  "tasks": [
    {
      // Fallback ohne Erweiterung: startet einen einfachen Webserver auf Port 8000.
      // Aufruf: Terminal → Task ausführen… → "Website starten"
      "label": "Website starten",
      "type": "shell",
      "command": "python3 -m http.server 8000",
      "windows": {
        "command": "python -m http.server 8000"
      },
      "isBackground": true,
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      },
      "problemMatcher": [],
      "runOptions": {
        "instanceLimit": 1
      },
      "detail": "Startet http://localhost:8000 — mit Strg+C im Terminal beenden."
    }
  ]
}
